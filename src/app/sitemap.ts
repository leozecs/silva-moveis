import type { MetadataRoute } from "next";
import { getStorefrontCategories, getStorefrontProducts } from "@/lib/medusa";
export default async function sitemap(): Promise<MetadataRoute.Sitemap> { const [products, categories] = await Promise.all([getStorefrontProducts({ limit: 100 }), getStorefrontCategories()]); const base = "https://silvamoveis.com.br"; return [{ url: base, changeFrequency: "daily", priority: 1 }, { url: `${base}/catalogo`, changeFrequency: "daily", priority: .9 }, { url: `${base}/sobre`, priority: .5 }, { url: `${base}/contato`, priority: .5 }, ...categories.map((category) => ({ url: `${base}/catalogo?categoria=${category.handle}`, priority: .7 })), ...products.map((product) => ({ url: `${base}/produto/${product.handle}`, changeFrequency: "weekly" as const, priority: .8 }))]; }
