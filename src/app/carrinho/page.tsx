"use client";
import Image from "next/image";
import Link from "next/link";
import { ArrowLeft, ArrowRight, Minus, Plus, ShoppingCart, Trash2 } from "lucide-react";
import { useCart } from "@/components/cart-provider";
import { money } from "@/lib/cart";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";

export default function CarrinhoPage() { const { cart, isLoading, error, updateQuantity, removeItem } = useCart(); const items = cart?.items ?? []; if (isLoading) return <main className="mx-auto max-w-5xl px-4 py-24 text-center text-muted-foreground">Carregando seu carrinho...</main>; if (!cart || !items.length) return <main className="mx-auto max-w-5xl px-4 py-14 sm:px-6 lg:px-8 lg:py-24"><div className="flex items-center gap-3"><ShoppingCart className="size-6 text-gold" /><p className="text-xs font-semibold uppercase tracking-[0.28em] text-gold">Carrinho</p></div><h1 className="mt-4 text-4xl font-semibold tracking-tight sm:text-5xl">Seu carrinho está vazio.</h1><p className="mt-5 max-w-xl text-base leading-7 text-muted-foreground">Encontre peças para compor seus ambientes e adicione seus favoritos aqui.</p><Link href="/catalogo" className="mt-8 inline-flex h-10 items-center gap-2 rounded-lg bg-primary px-4 text-sm font-medium text-primary-foreground">Explorar produtos<ArrowRight className="size-4" /></Link></main>; return <main className="mx-auto max-w-6xl px-4 py-12 sm:px-6 lg:px-8 lg:py-20"><Link href="/catalogo" className="inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4" />Continuar comprando</Link><h1 className="mt-6 text-4xl font-semibold tracking-tight">Seu carrinho</h1><div className="mt-10 grid gap-8 lg:grid-cols-[1fr_360px]"><div className="grid gap-4">{items.map((item) => <Card key={item.id}><CardContent className="flex gap-4 p-4 sm:p-5"><div className="relative size-24 shrink-0 overflow-hidden rounded-lg bg-secondary sm:size-32">{item.thumbnail ? <Image src={item.thumbnail} alt={item.title ?? "Produto"} fill sizes="128px" className="object-cover" unoptimized /> : null}</div><div className="min-w-0 flex-1"><p className="font-medium">{item.title}</p><p className="mt-2 text-sm text-muted-foreground">{money(item.unit_price, cart.currency_code)}</p><div className="mt-5 flex items-center gap-2"><Button size="icon-sm" variant="outline" onClick={() => void updateQuantity(item.id, item.quantity - 1)}><Minus className="size-3" /></Button><span className="min-w-6 text-center text-sm">{item.quantity}</span><Button size="icon-sm" variant="outline" onClick={() => void updateQuantity(item.id, item.quantity + 1)}><Plus className="size-3" /></Button></div></div><button aria-label="Remover produto" onClick={() => void removeItem(item.id)} className="self-start text-muted-foreground hover:text-destructive"><Trash2 className="size-4" /></button></CardContent></Card>)}{error ? <p className="text-sm text-destructive">{error}</p> : null}</div><Card className="h-fit"><CardContent className="p-6"><h2 className="text-lg font-semibold">Resumo do pedido</h2><div className="mt-6 flex justify-between text-sm text-muted-foreground"><span>Subtotal</span><span>{money(cart.subtotal, cart.currency_code)}</span></div><div className="mt-3 flex justify-between text-sm text-muted-foreground"><span>Entrega</span><span>A calcular</span></div><div className="mt-5 flex justify-between border-t border-border pt-5 text-lg font-semibold"><span>Total</span><span>{money(cart.total ?? cart.subtotal, cart.currency_code)}</span></div><Link href="/checkout" className="mt-6 inline-flex h-11 w-full items-center justify-center gap-2 rounded-lg bg-primary text-sm font-medium text-primary-foreground">Finalizar compra<ArrowRight className="size-4" /></Link></CardContent></Card></div></main>; }
